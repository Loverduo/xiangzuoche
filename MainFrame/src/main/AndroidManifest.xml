<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ruixiangtong.xiangzuoche" >

    <application
        android:name=".XzcApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:debuggable="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >


        <!--家环境-->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="p3MsDDM3nirPwg8tkuTmTqIzIGRMLioD" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".welcome.WelComePresenter" />
        <activity android:name=".splashscreen.SplashScreenPresenter">

        </activity>
    </application>

</manifest>
